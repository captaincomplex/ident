# Secrets / credentials — never commit
.env
.env.*
.cache-*
config.json
*.pem
credentials*
secrets*

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# OS
.DS_Store

# Runtime files (regenerated by the daemon)
status.json
current.png

# Large mockup/export artifacts (source is now in flightwall/) 
*.zip
*.mp4

# stray sandbox artifact
.writetest

# runtime caches
coastline.json
secret.key
